The actual NB Classifier code is contained within nbclassifier.py, and can be run like any other python file after running pip install -r requirements.txt

The file indexData.py is where the data splitting logic is located. If you wanted to achieve another data split to test the model yourself, you could alter the random seed.